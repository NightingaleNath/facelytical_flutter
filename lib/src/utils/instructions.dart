/// Contains all instruction text messages for the face capture process
class Instructions {
  /// When no face is detected in the frame
  static const String positionFace = "Position your face in the frame";

  /// When a face is detected and ready to capture
  static const String faceDetected = "Face detected, tap button to capture";

  /// When capture is in progress
  static const String capturing = "Capturing face, please hold still";
}
